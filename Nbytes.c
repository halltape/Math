#include <stdio.h>
#include <math.h>

int main ()
{
    // this program calculate the max of numbers from bytes //
    int N, numbers;
    scanf("%d", &N);
    numbers = pow(2, N);
    printf("%d", numbers);
    return 0;
}